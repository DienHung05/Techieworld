<?php
/**
 * PVModern – Modern customer login page (self-contained)
 * @var \Magento\Customer\Block\Form\Login $block
 * @var \Magento\Framework\Escaper        $escaper
 */
$actionUrl      = $escaper->escapeUrl($block->getPostActionUrl());
$forgotPostUrl  = $escaper->escapeUrl($block->getUrl('customer/account/forgotpasswordpost'));
$createPostUrl  = $escaper->escapeUrl($block->getUrl('customer/account/createpost'));
$savedUsername  = $escaper->escapeHtmlAttr((string) $block->getUsername());
$formKeyHtml    = $block->getBlockHtml('formkey');
$noAC           = $block->isAutocompleteDisabled() ? 'autocomplete="off"' : '';
$homeUrl        = $escaper->escapeUrl($block->getUrl(''));
$initialView    = (string) $block->getData('initial_view');
$initialView    = in_array($initialView, ['login', 'signup'], true) ? $initialView : 'login';
$showSignup     = $initialView === 'signup';

/* Capture flash messages (wrong password etc.) */
$serverMsgs = '';
try {
    $mb = $block->getLayout()->getBlock('page.messages');
    if ($mb) { $serverMsgs = $mb->toHtml(); }
} catch (\Throwable $e) {}
?>
<!-- ═══════════════════════════════════════════════════════ HTML ════ -->
<div class="pv-auth-page" id="pvAuthPage">

    <!-- ── LEFT: Hero ── -->
    <div class="pv-hero" aria-hidden="true">
        <div class="pv-hero-circle pv-hero-circle--1"></div>
        <div class="pv-hero-circle pv-hero-circle--2"></div>
        <div class="pv-hero-circle pv-hero-circle--3"></div>
        <div class="pv-hero-circle pv-hero-circle--4"></div>
        <div class="pv-hero-content">
            <a class="pv-hero-logo" href="<?= $homeUrl ?>" aria-label="Back to homepage" style="text-decoration:none;cursor:pointer;">Techie<span>World</span></a>
            <p class="pv-hero-tag">The premium destination for cutting-edge technology, components, and innovation.</p>
            <span class="pv-hero-badge">Trusted by 50,000+ customers</span>
        </div>
    </div>

    <!-- ── RIGHT: Form panel ── -->
    <div class="pv-form-panel">
        <div class="pv-card" id="pvCard">

            <!-- ════════════════ VIEW: LOGIN ════════════════ -->
            <div class="pv-view<?= $showSignup ? ' pv-view--hidden' : '' ?>" id="viewLogin">
                <!-- Server messages (wrong password etc.) -->
                <?php if ($serverMsgs): ?>
                <div class="pv-server-msgs"><?= $serverMsgs ?></div>
                <?php endif; ?>
                <!-- Cookie-based error surface: Magento stores post-redirect
                     login errors in the `mage-messages` cookie. The custom
                     template renders before the standard messages widget can
                     consume it, so a small inline script reads + clears it. -->
                <div id="pvCookieMsgs" class="pv-server-msgs" style="display:none;"></div>

                <h1 class="pv-heading">Welcome back</h1>
                <p class="pv-subtitle">Sign in to your account</p>

                <form id="loginForm" action="<?= $actionUrl ?>" method="post" novalidate>
                    <?= $formKeyHtml ?>
                    <?= $block->getChildHtml('form_additional_info') ?>

                    <!-- Username field -->
                    <div class="pv-field" id="fldUser">
                        <label for="pvUser">Username</label>
                        <div class="pv-input-wrap">
                            <input type="text" id="pvUser" name="login[username]"
                                   value="<?= $savedUsername ?>"
                                   placeholder="your username"
                                   autocomplete="username"
                                   <?= $noAC ?>
                                   aria-label="Username"
                                   aria-required="true"/>
                        </div>
                        <div class="pv-err" id="errUser"><svg viewBox="0 0 20 20" fill="currentColor" width="14" height="14" aria-hidden="true"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/></svg><span>Please enter your username.</span></div>
                    </div>

                    <!-- Password field -->
                    <div class="pv-field" id="fldPass">
                        <label for="pvPass">Password</label>
                        <div class="pv-input-wrap">
                            <input type="password" id="pvPass" name="login[password]"
                                   placeholder="••••••••"
                                   autocomplete="current-password"
                                   <?= $noAC ?>
                                   aria-label="Password"
                                   aria-required="true"/>
                            <button type="button" class="pv-eye" id="eyePass"
                                    aria-label="Show password" title="Show password">
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
                            </button>
                        </div>
                        <div class="pv-err" id="errPass"><svg viewBox="0 0 20 20" fill="currentColor" width="14" height="14" aria-hidden="true"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/></svg><span>Password must be at least 6 characters.</span></div>
                    </div>

                    <!-- Remember + Forgot row -->
                    <div class="pv-meta">
                        <label class="pv-remember">
                            <input type="checkbox" id="rememberMe" aria-label="Remember me"/>
                            <span>Remember me</span>
                        </label>
                        <button type="button" class="pv-link" onclick="pvSwitch('viewLogin','viewForgot')">Forgot password?</button>
                    </div>

                    <!-- Login button -->
                    <button type="submit" class="pv-btn" id="loginBtn">
                        <div class="pv-spinner" aria-hidden="true"></div>
                        <span class="pv-btn-label">Sign in</span>
                    </button>
                </form>

                <!-- Divider -->
                <div class="pv-divider">or continue with</div>

                <!-- Social buttons -->
                <div class="pv-socials" role="group" aria-label="Alternative sign-in methods">
                    <!-- Google -->
                    <button type="button" class="pv-social-btn" title="Sign in with Google" aria-label="Sign in with Google" onclick="pvSocialClick('google')">
                        <span class="pv-google-g"></span>
                    </button>
                    <!-- GitHub: octocat SVG -->
                    <button type="button" class="pv-social-btn pv-social-btn--github" title="Sign in with GitHub" aria-label="Sign in with GitHub" onclick="pvSocialClick('github')">
                        <svg viewBox="0 0 24 24" fill="currentColor" width="20" height="20" aria-hidden="true"><path d="M12 0C5.374 0 0 5.373 0 12c0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23A11.509 11.509 0 0 1 12 5.803c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576C20.566 21.797 24 17.3 24 12c0-6.627-5.373-12-12-12z"/></svg>
                    </button>
                    <!-- Email: envelope SVG -->
                    <button type="button" class="pv-social-btn pv-social-btn--email" title="Sign in with Email" aria-label="Sign in with Email" onclick="pvSwitch('viewLogin','viewEmail')">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="20" height="20" aria-hidden="true"><rect x="2" y="4" width="20" height="16" rx="2"/><polyline points="22,4 12,13 2,4"/></svg>
                    </button>
                    <!-- Phone: smartphone SVG -->
                    <button type="button" class="pv-social-btn pv-social-btn--phone" title="Sign in with Phone" aria-label="Sign in with Phone" onclick="pvSwitch('viewLogin','viewPhone')">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="20" height="20" aria-hidden="true"><rect x="5" y="2" width="14" height="20" rx="2"/><circle cx="12" cy="18" r="1" fill="currentColor" stroke="none"/><line x1="9" y1="6" x2="15" y2="6"/></svg>
                    </button>
                </div>

                <div class="pv-bottom-link">
                    Don't have an account?
                    <button type="button" class="pv-link" onclick="pvSwitch('viewLogin','viewSignup')">Sign up</button>
                </div>
            </div><!-- /viewLogin -->

            <!-- ════════════════ VIEW: EMAIL/USERNAME LOGIN ════════════════ -->
            <div class="pv-view pv-view--hidden" id="viewEmail">
                <h1 class="pv-heading">Sign in</h1>
                <p class="pv-subtitle">Use your username (or email) and password</p>

                <form id="emailLoginForm" action="<?= $actionUrl ?>" method="post" novalidate>
                    <?= $formKeyHtml ?>
                    <?= $block->getChildHtml('form_additional_info') ?>
                    <div class="pv-field" id="fldEmail2">
                        <label for="pvEmail2">Username or email</label>
                        <div class="pv-input-wrap">
                            <input type="text" id="pvEmail2" name="login[username]"
                                   placeholder="username or you@example.com" autocomplete="username"
                                   aria-label="Username or email" aria-required="true"/>
                        </div>
                        <div class="pv-err" id="errEmail2"><svg viewBox="0 0 20 20" fill="currentColor" width="14" height="14" aria-hidden="true"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/></svg><span>Please enter your username or email.</span></div>
                    </div>
                    <div class="pv-field" id="fldPass2">
                        <label for="pvPass2">Password</label>
                        <div class="pv-input-wrap">
                            <input type="password" id="pvPass2" name="login[password]"
                                   placeholder="••••••••" autocomplete="current-password"
                                   aria-label="Password" aria-required="true"/>
                            <button type="button" class="pv-eye" id="eyePass2" aria-label="Show password">
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
                            </button>
                        </div>
                        <div class="pv-err" id="errPass2"><svg viewBox="0 0 20 20" fill="currentColor" width="14" height="14" aria-hidden="true"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/></svg><span>Password must be at least 6 characters.</span></div>
                    </div>
                    <button type="submit" class="pv-btn" id="emailLoginBtn" style="margin-top:8px">
                        <div class="pv-spinner"></div>
                        <span class="pv-btn-label">Sign in</span>
                    </button>
                </form>
                <div class="pv-back-wrap"><button type="button" class="pv-link" onclick="pvSwitch('viewEmail','viewLogin')">← Back to sign in</button></div>
            </div><!-- /viewEmail -->

            <!-- ════════════════ VIEW: PHONE LOGIN ════════════════ -->
            <div class="pv-view pv-view--hidden" id="viewPhone">
                <h1 class="pv-heading">Phone sign in</h1>
                <p class="pv-subtitle">We'll send a one-time code to your number</p>

                <div id="phoneStep1">
                    <div class="pv-field" id="fldPhone">
                        <label for="pvPhone">Phone number</label>
                        <div class="pv-phone-row">
                            <div class="pv-flag">🇻🇳 +84</div>
                            <div class="pv-input-wrap">
                                <input type="tel" id="pvPhone" name="phone"
                                       placeholder="09x xxx xxxx" autocomplete="tel"
                                       aria-label="Phone number" aria-required="true"/>
                            </div>
                        </div>
                        <div class="pv-err" id="errPhone"><svg viewBox="0 0 20 20" fill="currentColor" width="14" height="14" aria-hidden="true"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/></svg><span>Please enter a valid phone number.</span></div>
                    </div>
                    <button type="button" class="pv-btn" id="sendOtpBtn" onclick="pvSendOtp()">
                        <div class="pv-spinner"></div>
                        <span class="pv-btn-label">Send OTP</span>
                    </button>
                </div>

                <div id="phoneStep2" style="display:none">
                    <p style="font-size:13px;color:var(--pv-muted);text-align:center;margin-bottom:16px">Enter the 6-digit code sent to <strong id="sentToNum"></strong></p>
                    <div class="pv-otp-row">
                        <input type="text" class="pv-otp-box" maxlength="1" inputmode="numeric" pattern="[0-9]" aria-label="OTP digit 1"/>
                        <input type="text" class="pv-otp-box" maxlength="1" inputmode="numeric" pattern="[0-9]" aria-label="OTP digit 2"/>
                        <input type="text" class="pv-otp-box" maxlength="1" inputmode="numeric" pattern="[0-9]" aria-label="OTP digit 3"/>
                        <input type="text" class="pv-otp-box" maxlength="1" inputmode="numeric" pattern="[0-9]" aria-label="OTP digit 4"/>
                        <input type="text" class="pv-otp-box" maxlength="1" inputmode="numeric" pattern="[0-9]" aria-label="OTP digit 5"/>
                        <input type="text" class="pv-otp-box" maxlength="1" inputmode="numeric" pattern="[0-9]" aria-label="OTP digit 6"/>
                    </div>
                    <p class="pv-otp-hint" id="otpTimer">Code expires in <strong id="otpCountdown">2:00</strong></p>
                    <button type="button" class="pv-btn" onclick="pvVerifyOtp()">
                        <div class="pv-spinner"></div>
                        <span class="pv-btn-label">Verify &amp; Sign in</span>
                    </button>
                    <div style="text-align:center;margin-top:12px">
                        <button type="button" class="pv-link" onclick="pvResendOtp()">Resend code</button>
                    </div>
                </div>

                <div class="pv-back-wrap"><button type="button" class="pv-link" onclick="pvSwitch('viewPhone','viewLogin')">← Back to sign in</button></div>
            </div><!-- /viewPhone -->

            <!-- ════════════════ VIEW: FORGOT PASSWORD ════════════════ -->
            <div class="pv-view pv-view--hidden" id="viewForgot">
                <div id="forgotForm">
                    <h1 class="pv-heading">Reset password</h1>
                    <p class="pv-subtitle">Enter your email and we'll send a reset link.</p>
                    <form id="forgotPwdForm" action="<?= $forgotPostUrl ?>" method="post" novalidate>
                        <?= $formKeyHtml ?>
                        <div class="pv-field" id="fldForgotEmail">
                            <label for="pvForgotEmail">Email address</label>
                            <div class="pv-input-wrap">
                                <input type="email" id="pvForgotEmail" name="email"
                                       placeholder="you@example.com" autocomplete="email"
                                       aria-label="Email address" aria-required="true"/>
                            </div>
                            <div class="pv-err" id="errForgotEmail"><svg viewBox="0 0 20 20" fill="currentColor" width="14" height="14" aria-hidden="true"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/></svg><span>Please enter a valid email.</span></div>
                        </div>
                        <button type="submit" class="pv-btn" id="forgotBtn">
                            <div class="pv-spinner"></div>
                            <span class="pv-btn-label">Send reset link</span>
                        </button>
                    </form>
                </div>
                <div id="forgotSuccess" class="pv-view pv-view--hidden">
                    <div class="pv-success-box">
                        <div class="pv-success-icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" width="26" height="26" aria-hidden="true"><polyline points="20 6 9 17 4 12"/></svg></div>
                        <h3>Check your email!</h3>
                        <p>We've sent a password reset link to<br><strong id="resetSentTo"></strong></p>
                    </div>
                </div>
                <div class="pv-back-wrap"><button type="button" class="pv-link" onclick="pvSwitch('viewForgot','viewLogin')">← Back to sign in</button></div>
            </div><!-- /viewForgot -->

            <!-- ════════════════ VIEW: SIGN UP ════════════════ -->
            <div class="pv-view<?= $showSignup ? '' : ' pv-view--hidden' ?>" id="viewSignup">
                <h1 class="pv-heading">Create account</h1>
                <p class="pv-subtitle">Join TechieWorld today — it's free</p>

                <form id="signupForm" action="<?= $createPostUrl ?>" method="post" novalidate>
                    <?= $formKeyHtml ?>

                    <div class="pv-signup-scroll">
                        <div class="pv-field" id="fldFirstName">
                            <label for="pvFirstName">First name</label>
                            <div class="pv-input-wrap">
                                <input type="text" id="pvFirstName" name="firstname" placeholder="John" autocomplete="given-name" aria-label="First name" aria-required="true"/>
                            </div>
                            <div class="pv-err" id="errFirstName"><svg viewBox="0 0 20 20" fill="currentColor" width="14" height="14" aria-hidden="true"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/></svg><span>First name is required.</span></div>
                        </div>

                        <div class="pv-field" id="fldLastName">
                            <label for="pvLastName">Last name</label>
                            <div class="pv-input-wrap">
                                <input type="text" id="pvLastName" name="lastname" placeholder="Smith" autocomplete="family-name" aria-label="Last name" aria-required="true"/>
                            </div>
                            <div class="pv-err" id="errLastName"><svg viewBox="0 0 20 20" fill="currentColor" width="14" height="14" aria-hidden="true"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/></svg><span>Last name is required.</span></div>
                        </div>

                        <div class="pv-field" id="fldRegEmail">
                            <label for="pvRegEmail">Email address</label>
                            <div class="pv-input-wrap">
                                <input type="email" id="pvRegEmail" name="email" placeholder="you@example.com" autocomplete="email" aria-label="Email address" aria-required="true"/>
                            </div>
                            <div class="pv-err" id="errRegEmail"><svg viewBox="0 0 20 20" fill="currentColor" width="14" height="14" aria-hidden="true"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/></svg><span>Please enter a valid email.</span></div>
                            <div class="pv-ok"  id="okRegEmail"><svg viewBox="0 0 20 20" fill="currentColor" width="14" height="14" aria-hidden="true"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/></svg><span>Looks good!</span></div>
                        </div>

                        <div class="pv-field" id="fldRegUser">
                            <label for="pvRegUser">Username</label>
                            <div class="pv-input-wrap">
                                <input type="text" id="pvRegUser" name="pv_username" placeholder="techie_user" autocomplete="username" aria-label="Username" aria-required="true"/>
                            </div>
                            <div class="pv-err" id="errRegUser"><svg viewBox="0 0 20 20" fill="currentColor" width="14" height="14" aria-hidden="true"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/></svg><span>Username is required (letters, numbers, _ . -, 3-32 chars).</span></div>
                        </div>

                        <div class="pv-field" id="fldRegPass">
                            <label for="pvRegPass">Password</label>
                            <div class="pv-input-wrap">
                                <input type="password" id="pvRegPass" name="password" placeholder="••••••••" autocomplete="new-password" aria-label="Password" aria-required="true" oninput="pvStrength(this.value);pvCheckMatch()"/>
                                <button type="button" class="pv-eye" id="eyeRegPass" aria-label="Show password">
                                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
                                </button>
                            </div>
                            <div class="pv-err" id="errRegPass"><svg viewBox="0 0 20 20" fill="currentColor" width="14" height="14" aria-hidden="true"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/></svg><span>Password must be at least 8 characters with letters and numbers.</span></div>
                            <div class="pv-strength" id="strengthWrap">
                                <div class="pv-strength-bar-wrap"><div class="pv-strength-bar" id="strengthBar"></div></div>
                                <div class="pv-strength-label" id="strengthLabel" aria-live="polite"></div>
                            </div>
                        </div>

                        <div class="pv-field" id="fldConfPass">
                            <label for="pvConfPass">Confirm password</label>
                            <div class="pv-input-wrap">
                                <input type="password" id="pvConfPass" name="password_confirmation" placeholder="••••••••" autocomplete="new-password" aria-label="Confirm password" aria-required="true" oninput="pvCheckMatch()"/>
                                <button type="button" class="pv-eye" id="eyeConfPass" aria-label="Show password">
                                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
                                </button>
                            </div>
                            <div class="pv-err" id="errConfPass"><svg viewBox="0 0 20 20" fill="currentColor" width="14" height="14" aria-hidden="true"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/></svg><span>Passwords do not match.</span></div>
                            <div class="pv-ok"  id="okConfPass"><svg viewBox="0 0 20 20" fill="currentColor" width="14" height="14" aria-hidden="true"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/></svg><span>Passwords match!</span></div>
                        </div>

                        <label class="pv-terms">
                            <input type="checkbox" id="termsCheck" aria-label="Agree to terms" onchange="pvToggleRegBtn()"/>
                            <span>I agree to the <a href="#" class="pv-link" style="font-size:13px">Terms of Service</a> and <a href="#" class="pv-link" style="font-size:13px">Privacy Policy</a></span>
                        </label>
                    </div>

                    <button type="submit" class="pv-btn" id="regBtn" disabled>
                        <div class="pv-spinner"></div>
                        <span class="pv-btn-label">Create account</span>
                    </button>
                </form>

                <div class="pv-bottom-link">
                    Already have an account?
                    <button type="button" class="pv-link" onclick="pvSwitch('viewSignup','viewLogin')">Sign in</button>
                </div>
            </div><!-- /viewSignup -->

        </div><!-- /pvCard -->
    </div><!-- /pv-form-panel -->
</div><!-- /pv-auth-page -->

<!-- ═══════════════════════════════════════════════════════ SCRIPT ══ -->
<script>
(function(){
'use strict';

/* ── PHP data passed to JS ── */
var FORGOT_POST  = '<?= $forgotPostUrl ?>';

/* ── Cookie-based error surface ──
   Magento stores post-redirect login errors (wrong password, CAPTCHA,
   invalid form key) in the `mage-messages` cookie. Render them here so
   users always see why a login attempt failed instead of a silent reload. */
(function(){
    var match = document.cookie.match(/(?:^|;\s*)mage-messages=([^;]+)/);
    if (!match) return;
    var raw;
    try { raw = decodeURIComponent(match[1]); } catch (e) { return; }
    var msgs;
    try { msgs = JSON.parse(raw); } catch (e) { return; }
    if (!Array.isArray(msgs) || !msgs.length) return;

    var box = document.getElementById('pvCookieMsgs');
    if (!box) return;
    // Dedupe identical messages — the cookie can accumulate repeats.
    var seen = {};
    var html = msgs.filter(function(m){
        var k = (m.type || 'error') + '|' + (m.text || '');
        if (seen[k]) return false;
        seen[k] = 1;
        return !!(m.text);
    }).map(function(m){
        var safe = String(m.text)
            .replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
        return '<div class="message message-' + (m.type || 'error') + ' ' + (m.type || 'error') + '">' + safe + '</div>';
    }).join('');
    if (!html) return;
    box.innerHTML = html;
    box.style.display = 'block';

    // Clear the cookie so the messages don't reappear on the next visit.
    document.cookie = 'mage-messages=; expires=Thu, 01 Jan 1970 00:00:00 GMT; path=/';
})();

/* ── Remember me: pre-fill on load ── */
(function(){
    var saved = localStorage.getItem('pvLoginUsername');
    if (saved) {
        var el = document.getElementById('pvUser');
        var cb = document.getElementById('rememberMe');
        if (el && !el.value) el.value = saved;
        if (cb) cb.checked = true;
    }
})();

/* ── View switching ── */
window.pvSwitch = function(fromId, toId) {
    var from = document.getElementById(fromId);
    var to   = document.getElementById(toId);
    if (!from || !to) return;

    from.style.transition = 'opacity 0.25s ease, transform 0.25s ease';
    from.style.opacity    = '0';
    from.style.transform  = 'translateY(-10px)';

    setTimeout(function(){
        from.classList.add('pv-view--hidden');
        from.style.opacity   = '';
        from.style.transform = '';

        to.classList.remove('pv-view--hidden');
        to.style.opacity   = '0';
        to.style.transform = 'translateY(12px)';

        requestAnimationFrame(function(){
            to.style.transition = 'opacity 0.3s ease, transform 0.3s ease';
            to.style.opacity    = '1';
            to.style.transform  = 'translateY(0)';
        });
    }, 250);
};

/* ── Password show/hide — SVG eye toggle ── */
var SVG_EYE      = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>';
var SVG_EYE_SLASH= '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16"><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"/><line x1="1" y1="1" x2="23" y2="23"/></svg>';

function pvToggleEye(inputId, btnId) {
    var input = document.getElementById(inputId);
    var btn   = document.getElementById(btnId);
    if (!input || !btn) return;
    btn.addEventListener('click', function(){
        var show = input.type === 'password'; /* true = currently hidden, about to show */
        input.type   = show ? 'text' : 'password';
        btn.innerHTML= show ? SVG_EYE_SLASH : SVG_EYE;
        btn.setAttribute('aria-label', show ? 'Hide password' : 'Show password');
    });
}
pvToggleEye('pvPass',    'eyePass');
pvToggleEye('pvPass2',   'eyePass2');
pvToggleEye('pvRegPass', 'eyeRegPass');
pvToggleEye('pvConfPass','eyeConfPass');

/* ── Validation helpers ── */
function isEmail(v) { return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v.trim()); }
function isPhone(v) { return /^[0-9\s\-+]{8,15}$/.test(v.trim()); }

function setValid(fieldId, ok) {
    var field = document.getElementById(fieldId);
    if (!field) return;
    field.classList.toggle('has-error', !ok);
    field.classList.toggle('has-ok',    ok);
    var input = field.querySelector('input');
    if (input) { input.classList.toggle('pv-invalid', !ok); input.classList.toggle('pv-valid', ok); }
}
function clearValidation(fieldId) {
    var field = document.getElementById(fieldId);
    if (!field) return;
    field.classList.remove('has-error','has-ok');
    var input = field.querySelector('input');
    if (input) { input.classList.remove('pv-invalid','pv-valid'); }
}

/* Attach blur validation */
function onBlurValidate(inputId, fieldId, fn) {
    var el = document.getElementById(inputId);
    if (el) {
        el.addEventListener('blur', function(){ fn(this.value); });
        el.addEventListener('input', function(){ clearValidation(fieldId); });
    }
}

/* Username validation: 3-32 chars, letters/numbers/_.- */
function isUsername(v) { return /^[a-zA-Z0-9_.\-]{3,32}$/.test((v||'').trim()); }
/* Login identifier: accept username OR email */
function isLoginId(v)  { return isUsername(v) || isEmail(v); }
/* Password rule (signup): >=8 chars, has letter AND digit */
function isStrongPassword(v) { return (v||'').length >= 8 && /[A-Za-z]/.test(v) && /[0-9]/.test(v); }

/* Login form */
onBlurValidate('pvUser', 'fldUser', function(v){
    setValid('fldUser', (v||'').trim().length > 0);
});
onBlurValidate('pvPass', 'fldPass', function(v){
    setValid('fldPass', (v||'').length > 0);
});

/* Email/username login form */
onBlurValidate('pvEmail2', 'fldEmail2', function(v){ setValid('fldEmail2', isLoginId(v)); });
onBlurValidate('pvPass2',  'fldPass2',  function(v){ setValid('fldPass2',  (v||'').length > 0); });

/* Phone form */
onBlurValidate('pvPhone', 'fldPhone', function(v){ setValid('fldPhone', isPhone(v)); });

/* Signup form */
onBlurValidate('pvFirstName', 'fldFirstName', function(v){ setValid('fldFirstName', (v||'').trim().length >= 1); });
onBlurValidate('pvLastName',  'fldLastName',  function(v){ setValid('fldLastName',  (v||'').trim().length >= 1); });
onBlurValidate('pvRegEmail',  'fldRegEmail',  function(v){ setValid('fldRegEmail',  isEmail(v)); });
onBlurValidate('pvRegUser',   'fldRegUser',   function(v){ setValid('fldRegUser',   isUsername(v)); });
onBlurValidate('pvRegPass',   'fldRegPass',   function(v){ setValid('fldRegPass',   isStrongPassword(v)); });

/* Forgot password */
onBlurValidate('pvForgotEmail', 'fldForgotEmail', function(v){ setValid('fldForgotEmail', isEmail(v)); });

/* ── Login form submit ── */
var loginForm = document.getElementById('loginForm');
if (loginForm) {
    loginForm.addEventListener('submit', function(e){
        var user = document.getElementById('pvUser').value;
        var pass = document.getElementById('pvPass').value;
        var ok   = true;

        if ((user||'').trim().length === 0) { setValid('fldUser', false); ok = false; }
        if ((pass||'').length === 0)        { setValid('fldPass', false); ok = false; }
        if (!ok) { e.preventDefault(); return; }

        /* Remember me */
        var rm = document.getElementById('rememberMe');
        if (rm && rm.checked) { localStorage.setItem('pvLoginUsername', user.trim()); }
        else                  { localStorage.removeItem('pvLoginUsername'); }

        /* Defer the loading state — disabling the submit button synchronously
           cancels native form submission in some browsers. */
        setTimeout(function(){ pvBtnLoading('loginBtn', true); }, 0);
    });
}

/* ── Email/username login form submit ── */
var emailLoginForm = document.getElementById('emailLoginForm');
if (emailLoginForm) {
    emailLoginForm.addEventListener('submit', function(e){
        var user = document.getElementById('pvEmail2').value;
        var pass = document.getElementById('pvPass2').value;
        var ok   = true;
        if (!isLoginId(user))      { setValid('fldEmail2', false); ok = false; }
        if ((pass||'').length===0) { setValid('fldPass2',  false); ok = false; }
        if (!ok) { e.preventDefault(); return; }
        setTimeout(function(){ pvBtnLoading('emailLoginBtn', true); }, 0);
    });
}

/* ── Forgot password form submit ── */
var forgotPwdForm = document.getElementById('forgotPwdForm');
if (forgotPwdForm) {
    forgotPwdForm.addEventListener('submit', function(e){
        e.preventDefault();
        var email = document.getElementById('pvForgotEmail').value.trim();
        if (!isEmail(email)) { setValid('fldForgotEmail', false); return; }

        pvBtnLoading('forgotBtn', true);
        /* POST to Magento's forgot password endpoint */
        var fd = new FormData(forgotPwdForm);
        fetch(FORGOT_POST, {
            method:'POST', body:fd, credentials:'same-origin',
            redirect:'manual'
        }).catch(function(){});

        /* Show success state after 1.5s */
        setTimeout(function(){
            pvBtnLoading('forgotBtn', false);
            document.getElementById('resetSentTo').textContent = email;
            document.getElementById('forgotForm').style.display = 'none';
            document.getElementById('forgotSuccess').classList.remove('pv-view--hidden');
        }, 1500);
    });
}

/* ── Loading state helper ── */
window.pvBtnLoading = function(btnId, on) {
    var btn = document.getElementById(btnId);
    if (!btn) return;
    if (on) { btn.classList.add('loading'); btn.disabled = true; }
    else    { btn.classList.remove('loading'); btn.disabled = false; }
};

/* ── Password strength meter ── */
window.pvStrength = function(val) {
    var bar   = document.getElementById('strengthBar');
    var label = document.getElementById('strengthLabel');
    if (!bar || !label) return;

    var score = 0;
    if (val.length >= 6)  score++;
    if (val.length >= 8)  score++;
    if (/[A-Z]/.test(val) && /[a-z]/.test(val)) score++;
    if (/[0-9]/.test(val)) score++;
    if (/[^A-Za-z0-9]/.test(val)) score++;

    var levels = [
        { pct:'20%', color:'#e74c3c', text:'Weak' },
        { pct:'40%', color:'#e67e22', text:'Fair' },
        { pct:'60%', color:'#f1c40f', text:'Good' },
        { pct:'80%', color:'#2ecc71', text:'Strong' },
        { pct:'100%',color:'#16a085', text:'Very strong' }
    ];
    var lvl = levels[Math.min(score, levels.length-1)];
    bar.style.width      = val.length ? lvl.pct : '0';
    bar.style.background = lvl.color;
    label.textContent    = val.length ? lvl.text : '';
    label.style.color    = lvl.color;
};

/* ── Confirm password match ── */
window.pvCheckMatch = function() {
    var pw  = document.getElementById('pvRegPass')  ? document.getElementById('pvRegPass').value  : '';
    var cpw = document.getElementById('pvConfPass') ? document.getElementById('pvConfPass').value : '';
    var fld = document.getElementById('fldConfPass');
    if (!fld || !cpw) return;
    if (cpw.length === 0) { clearValidation('fldConfPass'); return; }
    var match = (pw === cpw);
    fld.classList.toggle('has-error', !match);
    fld.classList.toggle('has-ok',     match);
    var input = fld.querySelector('input');
    if (input) { input.classList.toggle('pv-invalid', !match); input.classList.toggle('pv-valid', match); }
};

/* ── Signup button enable/disable ── */
window.pvToggleRegBtn = function() {
    var btn   = document.getElementById('regBtn');
    var terms = document.getElementById('termsCheck');
    if (btn && terms) btn.disabled = !terms.checked;
};

/* ── Signup form submit ── */
var signupForm = document.getElementById('signupForm');
if (signupForm) {
    signupForm.addEventListener('submit', function(e){
        var first = (document.getElementById('pvFirstName') || {}).value || '';
        var last  = (document.getElementById('pvLastName')  || {}).value || '';
        var email = (document.getElementById('pvRegEmail')  || {}).value || '';
        var user  = (document.getElementById('pvRegUser')   || {}).value || '';
        var pass  = (document.getElementById('pvRegPass')   || {}).value || '';
        var conf  = (document.getElementById('pvConfPass')  || {}).value || '';
        var ok    = true;

        if (first.trim().length < 1)   { setValid('fldFirstName', false); ok = false; }
        if (last.trim().length < 1)    { setValid('fldLastName',  false); ok = false; }
        if (!isEmail(email))           { setValid('fldRegEmail',  false); ok = false; }
        if (!isUsername(user))         { setValid('fldRegUser',   false); ok = false; }
        if (!isStrongPassword(pass))   { setValid('fldRegPass',   false); ok = false; }
        if (pass !== conf || conf===''){ setValid('fldConfPass',  false); ok = false; }

        if (!ok) { e.preventDefault(); return; }
        pvBtnLoading('regBtn', true);
        /* Native form POST — Magento's customer/account/createpost will redirect on success. */
    });
}

/* ── Social login click ── */
window.pvSocialClick = function(provider) {
    if (provider === 'github') {
        window.location.href = '<?= $block->escapeJs($block->getUrl('pvmodern/auth/github')) ?>';
        return;
    }
    var labels = { google:'Google' };
    alert('OAuth sign-in via ' + (labels[provider] || provider) + ' is not configured on this store. Please use email & password.');
};

/* ── Phone OTP flow ── */
var otpTimer;
window.pvSendOtp = function() {
    var phone = document.getElementById('pvPhone') ? document.getElementById('pvPhone').value.trim() : '';
    if (!isPhone(phone)) { setValid('fldPhone', false); return; }

    pvBtnLoading('sendOtpBtn', true);
    setTimeout(function(){
        pvBtnLoading('sendOtpBtn', false);
        document.getElementById('phoneStep1').style.display = 'none';
        document.getElementById('phoneStep2').style.display = 'block';
        document.getElementById('sentToNum').textContent = phone;
        pvStartOtpTimer(120);
        document.querySelectorAll('.pv-otp-box')[0].focus();
    }, 1500);
};

window.pvResendOtp = function() {
    clearInterval(otpTimer);
    pvStartOtpTimer(120);
    document.querySelectorAll('.pv-otp-box').forEach(function(b){ b.value = ''; });
    document.querySelectorAll('.pv-otp-box')[0].focus();
};

function pvStartOtpTimer(secs) {
    var countdown = document.getElementById('otpCountdown');
    clearInterval(otpTimer);
    otpTimer = setInterval(function(){
        secs--;
        var m = Math.floor(secs/60), s = secs%60;
        if (countdown) countdown.textContent = m + ':' + (s<10?'0':'') + s;
        if (secs <= 0) { clearInterval(otpTimer); if (countdown) countdown.textContent = '0:00'; }
    }, 1000);
}

window.pvVerifyOtp = function() {
    var boxes = document.querySelectorAll('.pv-otp-box');
    var code  = '';
    boxes.forEach(function(b){ code += b.value; });
    if (code.length < 6) { alert('Please enter the full 6-digit OTP.'); return; }
    /* In production: verify OTP via API */
    alert('OTP phone login is a demonstration. Please use email & password to sign in.');
};

/* OTP box auto-advance */
document.querySelectorAll('.pv-otp-box').forEach(function(box, i, all){
    box.addEventListener('input', function(){
        this.value = this.value.replace(/\D/g,'').slice(-1);
        if (this.value && i < all.length - 1) all[i+1].focus();
    });
    box.addEventListener('keydown', function(e){
        if (e.key === 'Backspace' && !this.value && i > 0) all[i-1].focus();
    });
});

})();
</script>
