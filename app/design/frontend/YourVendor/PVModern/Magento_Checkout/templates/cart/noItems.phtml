<?php

$priceHelper = $this->helper(Magento\Framework\Pricing\Helper\Data::class);
$continueShoppingUrl = $block->getUrl('', [
    '_query' => ['view' => 'products'],
    '_fragment' => 'pv3-products',
]);
?>
<div class="pvcart-empty-layout">
    <div class="pvcart-list-card">
        <div class="pvcart-empty">
            <div class="pvcart-empty-icon">🛒</div>
            <h3 class="pvcart-empty-title"><?= $escaper->escapeHtml(__('Your cart is empty')) ?></h3>
            <p class="pvcart-empty-sub"><?= $escaper->escapeHtml(__('Add products to your cart to continue shopping')) ?></p>
            <a class="pvcart-empty-btn"
               href="<?= $escaper->escapeUrl($continueShoppingUrl) ?>">
                <?= $escaper->escapeHtml(__('Continue shopping')) ?>
            </a>
        </div>
    </div>

    <aside class="pvcart-summary-card pvcart-summary-card--empty" aria-label="<?= $escaper->escapeHtmlAttr(__('Order summary')) ?>">
        <div class="pvcart-summary-head">
            <h2>
                <span><?= $escaper->escapeHtml(__('Order summary')) ?></span>
                <span class="pvcart-summary-count">0</span>
            </h2>
        </div>

        <div class="pvcart-summary-breakdown">
            <div class="pvcart-summary-row">
                <span><?= $escaper->escapeHtml(__('Subtotal (0 items)')) ?></span>
                <strong><?=  $priceHelper->currency(0, true, false) ?></strong>
            </div>
            <div class="pvcart-summary-row">
                <span><?= $escaper->escapeHtml(__('Shipping')) ?></span>
                <strong><?=  $priceHelper->currency(0, true, false) ?></strong>
            </div>
        </div>

        <div class="pvcart-summary-total">
            <div class="pvcart-summary-row pvcart-summary-row--total">
                <span><?= $escaper->escapeHtml(__('Grand total')) ?></span>
                <strong><?=  $priceHelper->currency(0, true, false) ?></strong>
            </div>
        </div>

        <div class="pvcart-summary-actions">
            <button type="button"
                    class="pvcart-primary-btn is-disabled"
                    disabled="disabled"
                    aria-disabled="true">
                <span><?= $escaper->escapeHtml(__('Proceed to checkout')) ?></span>
            </button>
            <a class="pvcart-secondary-btn"
               href="<?= $escaper->escapeUrl($continueShoppingUrl) ?>">
                <?= $escaper->escapeHtml(__('Continue shopping')) ?>
            </a>
        </div>
    </aside>
</div>
